# Force UTF-8 charset on all HTML responses
# Prevents mojibake (â€ artifacts) from server-side charset mismatches
/*.html
  Content-Type: text/html; charset=utf-8

/*
  X-Content-Type-Options: nosniff
